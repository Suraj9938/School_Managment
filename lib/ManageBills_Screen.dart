import 'package:flutter/material.dart';

class ManageBillsScreen extends StatelessWidget {
  static const routeName = 'managebills';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blueGrey,
      ),
    );
  }
}
