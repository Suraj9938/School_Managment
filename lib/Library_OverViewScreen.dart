import 'package:flutter/material.dart';

class LibraryOverViewScreen extends StatelessWidget {
  static const routeName = 'LibraryOverView';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
