import 'package:flutter/material.dart';

class StudentScreen extends StatelessWidget {
  static const routeName = 'studScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
